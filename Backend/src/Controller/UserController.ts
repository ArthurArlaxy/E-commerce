import type { Handler } from "express";

export class AuthController {
    constructor(){}

    login: Handler = async (req, res, next) =>  {
        try {
            
            const user = await 


        } catch (error) {
            
        }
    }
}